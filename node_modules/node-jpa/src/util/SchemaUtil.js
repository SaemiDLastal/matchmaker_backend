module.exports = {
  getPrimaryKey(schema) {
    return schema.fields[0].name
  },
  hasField(schema, name) {
    return schema.fields.some(item => item.name === name)
  },
  setPrimaryValue(schema, entity, value) {
    let key = this.getPrimaryKey(schema)
    entity[key] = value
  }
}
